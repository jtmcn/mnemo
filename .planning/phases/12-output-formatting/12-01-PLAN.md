---
phase: 12-output-formatting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mnemo/mcp/tools.py
  - tests/test_mcp.py
autonomous: false
requirements:
  - TOOL-02
must_haves:
  truths:
    - "Enriched search results visually distinguish matched chunks from context neighbors via separator lines and position labels"
    - "A human reviewing raw markdown in Claude Desktop can identify the matched chunk at a glance without reading content"
    - "context_window=0 output is unchanged (no enrichment markers)"
  artifacts:
    - path: "src/mnemo/mcp/tools.py"
      provides: "_format_enriched_results with --- separators and [MATCH]/[Context] labels on own lines"
      contains: "MATCH"
    - path: "tests/test_mcp.py"
      provides: "Updated assertions in TestSearchBooksContextWindow matching new marker format"
      contains: "MATCH"
  key_links:
    - from: "src/mnemo/mcp/tools.py"
      to: "tests/test_mcp.py"
      via: "TestSearchBooksContextWindow assertions match _format_enriched_results output"
      pattern: "MATCH.*seq"
---

<objective>
Strengthen visual delineation of matched vs. context chunks in enriched search results.

Purpose: When Claude Desktop renders search_books output with context_window >= 1, the matched chunk must be immediately identifiable at a glance via horizontal rule separators and bold/italic position labels on their own lines.

Output: Updated `_format_enriched_results` function and passing tests with new marker format.
</objective>

<execution_context>
@/Users/joel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-output-formatting/12-RESEARCH.md

<interfaces>
<!-- Key function and test class the executor needs -->

From src/mnemo/mcp/tools.py (lines 475-517):
```python
def _format_enriched_results(expanded_results: list[dict]) -> str:
    # expanded_results is list of dicts with keys:
    #   "result": SearchResult (has book_title, section_path, content_type, source, book_id)
    #   "chunks": list[Chunk] (has id, sequence, content, content_type)
    #   "matched_chunk_ids": set[str]
```

From tests/test_mcp.py (lines 1587-1693):
```python
class TestSearchBooksContextWindow:
    # test_search_books_context_window_zero_unchanged — MUST NOT regress
    # test_search_books_context_window_formats_enriched — assertions need updating
    # test_search_books_context_window_clamped — no assertion changes needed
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update enriched result formatter and tests</name>
  <files>src/mnemo/mcp/tools.py, tests/test_mcp.py</files>
  <behavior>
    - Test: context_window=1 output contains "MATCH — seq 1" (bold match label)
    - Test: context_window=1 output contains "Context — seq 0" and "Context — seq 2" (italic context labels)
    - Test: context_window=1 output contains "---" separator rules between chunks
    - Test: context_window=0 output still has NO "MATCHED" or "MATCH" markers (regression guard)
    - Test: context_window=5 clamping to 3 unchanged
  </behavior>
  <action>
    1. First update the 3 test assertions in TestSearchBooksContextWindow to expect the new format:
       - test_search_books_context_window_formats_enriched: change assertions from `"MATCHED (seq 1)"` to `"MATCH \u2014 seq 1"`, from `"[context, seq 0]"` to `"Context \u2014 seq 0"`, from `"[context, seq 2]"` to `"Context \u2014 seq 2"`. Add assertion that `"---"` appears in output.
       - test_search_books_context_window_zero_unchanged: add assertion `"MATCH" not in result` (in addition to existing `"MATCHED" not in result`) to guard against new label format. Also assert `"Context \u2014" not in result`.
       - test_search_books_context_window_clamped: no changes needed (tests clamping, not format).

    2. Run tests — confirm RED (the enriched format test fails because the code still emits old markers).

    3. Update `_format_enriched_results` in src/mnemo/mcp/tools.py:
       - Remove the blank line after the result header (line `lines.append("")` after Book ID line)
       - In the chunk loop, before each chunk, append:
         - `""` (blank line)
         - `"---"` (horizontal rule separator)
         - Match label: `f"**[MATCH \u2014 seq {chunk.sequence}]**"` if chunk.id in matched_ids
         - Context label: `f"*[Context \u2014 seq {chunk.sequence}]*"` if not matched
         - `""` (blank line after label, before content)
       - Remove the trailing `lines.append("")` after each chunk content (the next chunk's blank+separator handles spacing)
       - After the inner chunk loop, append a single `""` for spacing before the next result block

       Do NOT modify `_format_search_results` (the context_window=0 path). Only change `_format_enriched_results`.

    4. Run tests — confirm GREEN.

    Target output structure per result:
    ```
    ---
    **Source:** Book > Section
    **Book ID:** `id` | **Type:** text | **Match:** keyword

    ---
    *[Context — seq 0]*

    Content of context chunk...

    ---
    **[MATCH — seq 1]**

    Content of matched chunk...

    ---
    *[Context — seq 2]*

    Content of context chunk...

    ```
  </action>
  <verify>
    <automated>cd /Users/joel/Code/mnemo && python -m pytest tests/test_mcp.py::TestSearchBooksContextWindow -v</automated>
  </verify>
  <done>All 3 tests in TestSearchBooksContextWindow pass. Enriched output uses --- separators and [MATCH]/[Context] labels on own lines. context_window=0 path is unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual QA in Claude Desktop</name>
  <action>Human verifies rendered markdown output in Claude Desktop. What was built: updated enriched search result formatting with horizontal rule separators and bold/italic position labels for matched vs. context chunks.</action>
  <how-to-verify>
    1. Open Claude Desktop with the mnemo MCP server connected
    2. Run a search query with context: ask Claude to search for something in a book you have indexed, using context_window=1 or higher
    3. Verify in the rendered output:
       a. Each chunk boundary has a visible horizontal rule separator
       b. Matched chunks show a bold **[MATCH — seq N]** label on its own line
       c. Context chunks show an italic *[Context — seq N]* label on its own line
       d. The matched chunk is immediately identifiable at a glance without reading content
    4. Run a search without context (context_window=0 or default) and verify the output looks the same as before (no separators or labels)
  </how-to-verify>
  <verify>Human visual inspection in Claude Desktop</verify>
  <done>User confirms matched chunks are visually distinct from context chunks in rendered markdown</done>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/joel/Code/mnemo && python -m pytest tests/test_mcp.py -v
```
Full MCP test suite passes with no regressions.
</verification>

<success_criteria>
- All 3 tests in TestSearchBooksContextWindow pass with updated assertions
- Full test_mcp.py suite passes (no regressions in TestOutputFormatting or other classes)
- Visual confirmation in Claude Desktop that matched chunks are identifiable at a glance
</success_criteria>

<output>
After completion, create `.planning/phases/12-output-formatting/12-01-SUMMARY.md`
</output>
