---
phase: 17-epub-content-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mnemo/epub/_models.py
  - src/mnemo/epub/_classify.py
  - src/mnemo/epub/_extract.py
  - src/mnemo/epub/content.py
  - pyproject.toml
autonomous: true
requirements: [STRC-02]

must_haves:
  truths:
    - "All imports from mnemo.epub.content continue to resolve (ContentBlock, extract_content, _extract_math)"
    - "All imports from mnemo.epub continue to resolve (EPUBParser, ContentBlock, extract_content, extract_metadata)"
    - "No single file in epub/ exceeds ~460 lines (hard ceiling for _extract.py) and most are under 200"
    - "All 367+ existing tests pass with zero changes to test files"
  artifacts:
    - path: "src/mnemo/epub/_models.py"
      provides: "ContentBlock dataclass, all constants (FRONT_MATTER_STEMS, CODE_CLASSES, DIAGRAM_CLASSES, MATH_CLASSES, LATEX patterns, _KNOWN_LANGUAGES, MATHML_ELEMENTS)"
      contains: "class ContentBlock"
    - path: "src/mnemo/epub/_classify.py"
      provides: "Classification predicates (_is_code_block, _is_diagram, _looks_like_ascii_art, _is_math, _detect_code_language)"
      contains: "def _is_code_block"
    - path: "src/mnemo/epub/_extract.py"
      provides: "Extraction logic (extract_content, _extract_blocks_from_element, _extract_code_block, _extract_math, _table_to_text, _normalize_text, _infer_front_matter_label)"
      contains: "def extract_content"
    - path: "src/mnemo/epub/content.py"
      provides: "Backward-compatible re-export shim"
      contains: "from mnemo.epub._models import ContentBlock"
  key_links:
    - from: "src/mnemo/epub/_classify.py"
      to: "src/mnemo/epub/_models.py"
      via: "imports constants (CODE_CLASSES, DIAGRAM_CLASSES, MATH_CLASSES, LATEX patterns, _KNOWN_LANGUAGES)"
      pattern: "from mnemo\\.epub\\._models import"
    - from: "src/mnemo/epub/_extract.py"
      to: "src/mnemo/epub/_models.py"
      via: "imports ContentBlock, FRONT_MATTER_STEMS, MATHML_ELEMENTS"
      pattern: "from mnemo\\.epub\\._models import"
    - from: "src/mnemo/epub/_extract.py"
      to: "src/mnemo/epub/_classify.py"
      via: "imports classification predicates"
      pattern: "from mnemo\\.epub\\._classify import"
    - from: "src/mnemo/epub/content.py"
      to: "src/mnemo/epub/_extract.py"
      via: "re-exports extract_content and _extract_math"
      pattern: "from mnemo\\.epub\\._extract import extract_content"
    - from: "src/mnemo/epub/content.py"
      to: "src/mnemo/epub/_models.py"
      via: "re-exports ContentBlock"
      pattern: "from mnemo\\.epub\\._models import ContentBlock"
---

<objective>
Split epub/content.py (764 lines) into three focused private modules plus a backward-compatible re-export shim, satisfying STRC-02.

Purpose: Reduce module size below ~400 lines each, separate classification/extraction/data concerns, make the epub package easier to maintain for Phase 18.
Output: Three new files (_models.py, _classify.py, _extract.py), content.py reduced to ~15-line shim, version bumped to 1.8.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-epub-content-split/17-RESEARCH.md

@src/mnemo/epub/content.py
@src/mnemo/epub/__init__.py
@src/mnemo/epub/parser.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three private submodules from content.py</name>
  <files>src/mnemo/epub/_models.py, src/mnemo/epub/_classify.py, src/mnemo/epub/_extract.py</files>
  <read_first>
    src/mnemo/epub/content.py
    src/mnemo/epub/__init__.py
    src/mnemo/epub/parser.py
    src/mnemo/chunking/chunker.py
    tests/test_epub_parser.py
  </read_first>
  <action>
Create three new files by extracting code from content.py. Do NOT modify content.py yet (that happens in Task 2). The import chain must be: _models.py (no internal imports) -> _classify.py (imports _models) -> _extract.py (imports _models + _classify).

**File 1: `src/mnemo/epub/_models.py` (~80 lines)**

Move these items from content.py (lines 1-103):
- Module docstring: `"""Data models and constants for EPUB content extraction."""`
- Imports needed: `from __future__ import annotations`, `import re`, `from dataclasses import dataclass, field`, `from pathlib import Path`, `from typing import TYPE_CHECKING`, `from mnemo.models import ContentType`
- The TYPE_CHECKING block with `if TYPE_CHECKING: from bs4 import Tag` (only if ContentBlock uses Tag in type hints — check first)
- `FRONT_MATTER_STEMS` dict (lines 25-50)
- `ContentBlock` dataclass (lines 52-72)
- `CODE_CLASSES` set (lines 74-93)
- `DIAGRAM_CLASSES` set (line 95)
- `MATH_CLASSES` set (line 98)
- `LATEX_BLOCK_PATTERN` compiled regex (line 101)
- `LATEX_INLINE_PATTERN` compiled regex (line 102)
- `_KNOWN_LANGUAGES` set (lines 618-644) — yes, move this here even though it appears later in content.py, because _classify.py needs it
- `MATHML_ELEMENTS` frozenset (lines 646-667)

Do NOT include `import warnings`, `from bs4 import BeautifulSoup, NavigableString, Tag, XMLParsedAsHTMLWarning`, or the `warnings.filterwarnings` call — those stay with the modules that need them.

**File 2: `src/mnemo/epub/_classify.py` (~160 lines)**

Move these functions from content.py:
- Module docstring: `"""Content type classification predicates for EPUB HTML elements."""`
- Imports: `from __future__ import annotations`, `from bs4 import Tag`, `from mnemo.epub._models import (CODE_CLASSES, DIAGRAM_CLASSES, MATH_CLASSES, LATEX_BLOCK_PATTERN, LATEX_INLINE_PATTERN, _KNOWN_LANGUAGES, MATHML_ELEMENTS)`
- `_is_code_block(element: Tag) -> bool` (lines 409-450)
- `_is_diagram(element: Tag) -> bool` (lines 452-473)
- `_looks_like_ascii_art(text: str) -> bool` (lines 475-511)
- `_is_math(element: Tag) -> bool` (lines 513-539)
- `_detect_code_language(element: Tag) -> str | None` (lines 574-616)

Preserve all docstrings and comments exactly as they appear in content.py.

**File 3: `src/mnemo/epub/_extract.py` (~460 lines)**

Move these functions from content.py:
- Module docstring: `"""Content extraction logic for EPUB HTML items."""`
- Imports: `from __future__ import annotations`, `import re`, `import warnings`, `from pathlib import Path`, `from typing import TYPE_CHECKING`, `from bs4 import BeautifulSoup, NavigableString, Tag, XMLParsedAsHTMLWarning`, `from mnemo.models import ContentType`, `from mnemo.epub._models import ContentBlock, FRONT_MATTER_STEMS, MATHML_ELEMENTS, LATEX_BLOCK_PATTERN, LATEX_INLINE_PATTERN`, `from mnemo.epub._classify import _is_code_block, _is_diagram, _is_math, _detect_code_language, _looks_like_ascii_art`
- The `warnings.filterwarnings("ignore", category=XMLParsedAsHTMLWarning)` call
- `if TYPE_CHECKING: import ebooklib` block (if present in content.py)
- `_infer_front_matter_label(href: str) -> list[str] | None` (lines 105-126)
- `extract_content(epub_book, toc_mapping, default_language)` (lines 128-195)
- `_extract_blocks_from_element(element, section_path, source_file, default_language)` (lines 197-407)
- `_extract_code_block(element, section_path, source_file, default_language)` (lines 541-572)
- `_extract_math(element: Tag) -> str` (lines 670-699)
- `_table_to_text(table: Tag) -> str` (lines 701-751)
- `_normalize_text(text: str) -> str` (lines 753-764)

After creating all three files, verify each one can be parsed by Python:
```
uv run python -c "import ast; ast.parse(open('src/mnemo/epub/_models.py').read()); print('_models OK')"
uv run python -c "import ast; ast.parse(open('src/mnemo/epub/_classify.py').read()); print('_classify OK')"
uv run python -c "import ast; ast.parse(open('src/mnemo/epub/_extract.py').read()); print('_extract OK')"
```
  </action>
  <verify>
    <automated>uv run python -c "from mnemo.epub._models import ContentBlock, CODE_CLASSES, FRONT_MATTER_STEMS; print('_models imports OK')" && uv run python -c "from mnemo.epub._classify import _is_code_block, _is_diagram, _is_math; print('_classify imports OK')" && uv run python -c "from mnemo.epub._extract import extract_content, _extract_math; print('_extract imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/mnemo/epub/_models.py exists and contains `class ContentBlock`
    - src/mnemo/epub/_models.py contains `FRONT_MATTER_STEMS`, `CODE_CLASSES`, `DIAGRAM_CLASSES`, `MATH_CLASSES`
    - src/mnemo/epub/_models.py contains `_KNOWN_LANGUAGES` and `MATHML_ELEMENTS`
    - src/mnemo/epub/_classify.py exists and contains `def _is_code_block(`
    - src/mnemo/epub/_classify.py contains `from mnemo.epub._models import`
    - src/mnemo/epub/_classify.py does NOT contain `from mnemo.epub._extract`
    - src/mnemo/epub/_extract.py exists and contains `def extract_content(`
    - src/mnemo/epub/_extract.py contains `from mnemo.epub._models import` and `from mnemo.epub._classify import`
    - `uv run python -c "from mnemo.epub._extract import extract_content"` exits 0
  </acceptance_criteria>
  <done>Three new private modules exist, each importable without errors, with the correct one-way dependency chain: _models -> _classify -> _extract</done>
</task>

<task type="auto">
  <name>Task 2: Convert content.py to re-export shim, bump version, run full test suite</name>
  <files>src/mnemo/epub/content.py, pyproject.toml</files>
  <read_first>
    src/mnemo/epub/content.py
    src/mnemo/epub/__init__.py
    src/mnemo/epub/_models.py
    src/mnemo/epub/_classify.py
    src/mnemo/epub/_extract.py
    tests/test_epub_parser.py
    tests/test_chunker.py
    pyproject.toml
  </read_first>
  <action>
**Step 1: Replace content.py with re-export shim**

Replace the entire contents of `src/mnemo/epub/content.py` with exactly this:

```python
"""Backward-compatible re-exports from epub.content submodules.

All public and test-referenced symbols re-exported here so that existing
imports from mnemo.epub.content continue to work unchanged.
"""

from mnemo.epub._extract import _extract_math, extract_content  # noqa: F401
from mnemo.epub._models import ContentBlock  # noqa: F401

__all__ = ["ContentBlock", "extract_content"]
```

This preserves all three imports used across the codebase:
- `ContentBlock` — used by parser.py, chunker.py, __init__.py, tests
- `extract_content` — used by parser.py, __init__.py
- `_extract_math` — used by tests/test_epub_parser.py line 11

**Step 2: Bump version in pyproject.toml**

Change `version = "1.8.0"` to `version = "1.8.1"` in pyproject.toml. This is a PATCH bump (internal restructuring, no behavior changes).

**Step 3: Run the targeted test suite**

```
uv run pytest tests/test_epub_parser.py tests/test_chunker.py -x -q
```

This validates that all epub-related imports and behaviors still work.

**Step 4: Run the full test suite**

```
uv run pytest -x -q
```

All 367+ tests must pass.

**Step 5: Verify line counts**

```
wc -l src/mnemo/epub/_models.py src/mnemo/epub/_classify.py src/mnemo/epub/_extract.py src/mnemo/epub/content.py
```

Expected: _models.py ~80, _classify.py ~160, _extract.py ~460, content.py ~12. No file should exceed 460 lines.

**Step 6: Run linting**

```
uv run ruff check src/mnemo/epub/_models.py src/mnemo/epub/_classify.py src/mnemo/epub/_extract.py src/mnemo/epub/content.py
```

Fix any ruff errors. The `# noqa: F401` comments on the shim imports suppress the expected "imported but unused" warnings.
  </action>
  <verify>
    <automated>uv run pytest -x -q && uv run ruff check src/mnemo/epub/ && wc -l src/mnemo/epub/_models.py src/mnemo/epub/_classify.py src/mnemo/epub/_extract.py src/mnemo/epub/content.py</automated>
  </verify>
  <acceptance_criteria>
    - src/mnemo/epub/content.py contains `from mnemo.epub._models import ContentBlock  # noqa: F401`
    - src/mnemo/epub/content.py contains `from mnemo.epub._extract import _extract_math, extract_content  # noqa: F401`
    - src/mnemo/epub/content.py contains `__all__ = ["ContentBlock", "extract_content"]`
    - src/mnemo/epub/content.py is under 20 lines
    - pyproject.toml contains `version = "1.8.1"`
    - `uv run pytest -x -q` exits 0 with all tests passing
    - `uv run ruff check src/mnemo/epub/` exits 0
    - `wc -l src/mnemo/epub/_extract.py` shows fewer than 470 lines
  </acceptance_criteria>
  <done>content.py is a ~12-line re-export shim, all tests pass, ruff clean, version bumped to 1.8.1, no file in epub/ exceeds ~460 lines</done>
</task>

</tasks>

<verification>
1. Import compatibility: `uv run python -c "from mnemo.epub.content import ContentBlock, extract_content, _extract_math; print('OK')"`
2. Package-level imports: `uv run python -c "from mnemo.epub import EPUBParser, ContentBlock, extract_content; print('OK')"`
3. Full test suite: `uv run pytest -x -q` — all tests pass
4. Line counts: `wc -l src/mnemo/epub/*.py` — no file exceeds ~460 lines
5. Lint: `uv run ruff check src/mnemo/epub/` — clean
</verification>

<success_criteria>
- STRC-02 satisfied: epub/content.py split into focused modules, no single file exceeds ~400 lines (with ~460 allowed for _extract.py per research's "~400 allows flex")
- All existing imports from mnemo.epub.content resolve without changes to callers
- All 367+ tests pass with zero modifications to test files
- Version bumped to 1.8.1
</success_criteria>

<output>
After completion, create `.planning/phases/17-epub-content-split/17-01-SUMMARY.md`
</output>
