---
phase: 18-mcp-service-layer-refactor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mnemo/services/__init__.py
  - src/mnemo/services/book_service.py
  - src/mnemo/mcp/tools_search.py
  - src/mnemo/mcp/tools_books.py
  - src/mnemo/mcp/tools_metadata.py
  - src/mnemo/mcp/formatters.py
  - src/mnemo/mcp/server.py
autonomous: true
requirements: [STRC-01, STRC-04]

must_haves:
  truths:
    - "MCP tools are organized into three domain modules (search, books, metadata) plus formatters"
    - "No single file in mcp/ exceeds ~400 lines"
    - "Shared validation logic (epub path, duplicate detection) lives in services/book_service.py"
    - "All MCP tool registrations still work via @mcp.tool decorator"
  artifacts:
    - path: "src/mnemo/services/__init__.py"
      provides: "Services package init"
    - path: "src/mnemo/services/book_service.py"
      provides: "Shared business logic for CLI and MCP"
      exports: ["validate_epub_path", "find_duplicate", "list_all_books"]
    - path: "src/mnemo/mcp/tools_search.py"
      provides: "Search domain tools"
      contains: "_search_books_impl"
    - path: "src/mnemo/mcp/tools_books.py"
      provides: "Book management domain tools"
      contains: "_add_book_impl"
    - path: "src/mnemo/mcp/tools_metadata.py"
      provides: "Metadata domain tools"
      contains: "_list_available_books_impl"
    - path: "src/mnemo/mcp/formatters.py"
      provides: "Search result formatters extracted from tools_search"
      contains: "_format_search_results"
  key_links:
    - from: "src/mnemo/mcp/server.py"
      to: "tools_search, tools_books, tools_metadata"
      via: "import side-effect registration"
      pattern: "import mnemo\\.mcp\\.tools_"
    - from: "src/mnemo/mcp/tools_search.py"
      to: "src/mnemo/mcp/formatters.py"
      via: "import of formatter functions"
      pattern: "from mnemo\\.mcp\\.formatters import"
---

<objective>
Split mcp/tools.py (1,233 lines) into domain-specific modules and extract shared business logic into a service layer.

Purpose: Satisfy STRC-01 (no file > ~400 lines, domain split) and STRC-04 (service layer for shared CLI/MCP logic). This is the structural foundation that Plan 02 builds the DI refactor on top of.

Output: Four new mcp/ domain modules, one formatters module, one services/ package with book_service.py. Original tools.py content fully distributed. server.py updated to import new modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-mcp-service-layer-refactor/18-RESEARCH.md
@.planning/phases/17-epub-content-split/17-01-SUMMARY.md

@src/mnemo/mcp/tools.py
@src/mnemo/mcp/server.py
@src/mnemo/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service layer and split tools.py into domain modules</name>
  <files>
    src/mnemo/services/__init__.py,
    src/mnemo/services/book_service.py,
    src/mnemo/mcp/tools_search.py,
    src/mnemo/mcp/tools_books.py,
    src/mnemo/mcp/tools_metadata.py,
    src/mnemo/mcp/formatters.py
  </files>
  <read_first>
    src/mnemo/mcp/tools.py,
    src/mnemo/mcp/server.py,
    src/mnemo/cli.py,
    src/mnemo/mcp/__init__.py
  </read_first>
  <action>
**Step 1 — Create services package:**

Create `src/mnemo/services/__init__.py` (empty, just docstring):
```python
"""Service layer — shared business logic for CLI and MCP."""
```

Create `src/mnemo/services/book_service.py` with these exact functions:
```python
"""Shared book operations used by both CLI and MCP layers."""
from pathlib import Path
from mnemo.storage import BookRepository
from mnemo.models import Book

def validate_epub_path(path: Path) -> str | None:
    """Validate an EPUB file path. Returns error message or None if valid."""
    if not path.exists():
        return f"File not found: {path}"
    if path.suffix.lower() != ".epub":
        return f"Not an EPUB file: {path} (expected .epub extension)"
    return None

def find_duplicate(book_repo: BookRepository, file_hash: str) -> Book | None:
    """Check for an existing book with the same file hash."""
    return book_repo.get_by_hash(file_hash)

def list_all_books(book_repo: BookRepository) -> list[Book]:
    """Return all indexed books."""
    return book_repo.list_all()
```

**Step 2 — Create formatters.py:**

Extract these functions from tools.py (lines ~591-812) into `src/mnemo/mcp/formatters.py`:
- `_format_search_results` (with its full body)
- `_format_enriched_results` (with its full body)
- `_format_mixed_results` (with its full body)
- `_truncate_at_boundary` (with its full body)

Include the necessary imports at the top of formatters.py. These functions are pure string formatters — they take data and return markdown strings. They do not import from server.py or use globals.

**Step 3 — Create tools_search.py:**

Move from tools.py into `src/mnemo/mcp/tools_search.py`:
- All imports needed by search tools
- `from mnemo.mcp.server import mcp` (for @mcp.tool decorator)
- The globals: `_search_service`, `_db_connection` (temporarily — Plan 02 will DI-refactor these)
- The factory functions: `_get_search_service`, `_get_book_repo`, `_get_chunk_repo` (temporarily)
- `_search_books_impl` and `search_books` (the @mcp.tool wrapper)
- `_get_book_structure_impl` and `get_book_structure`
- `_get_book_chunks_impl` and `get_book_chunks`
- Import formatters: `from mnemo.mcp.formatters import _format_search_results, _format_enriched_results, _format_mixed_results, _truncate_at_boundary`

**Step 4 — Create tools_books.py:**

Move from tools.py into `src/mnemo/mcp/tools_books.py`:
- All imports needed by book management tools
- `from mnemo.mcp.server import mcp`
- Duplicate the globals and factory functions (same `_search_service`, `_db_connection`, `_get_search_service`, `_get_book_repo`, `_get_chunk_repo` — whichever ones these tools need). Yes, this means globals exist in multiple files temporarily — Plan 02 removes them via DI.
- `_add_book_impl` and `add_book`
- `_remove_book_impl` and `remove_book`
- `_reindex_all_books_impl` and `reindex_all_books`

**Step 5 — Create tools_metadata.py:**

Move from tools.py into `src/mnemo/mcp/tools_metadata.py`:
- All imports needed by metadata tools
- `from mnemo.mcp.server import mcp`
- Duplicate the globals and factory functions as needed
- `_list_available_books_impl` and `list_available_books`
- `_get_book_info_impl` and `get_book_info`
- `_update_book_metadata_impl` and `update_book_metadata`
- `_enrich_book_impl` and `enrich_book`

**CRITICAL NOTES:**
- Each domain module MUST import `from mnemo.mcp.server import mcp` to use the `@mcp.tool` decorator.
- Copy ALL type hints, TYPE_CHECKING imports, and docstrings exactly as they are in tools.py.
- The `asyncio.to_thread` pattern in `add_book` MUST be preserved exactly.
- The `CurrentContext` / `Context` parameters in tool wrappers MUST be preserved.
- Every `@mcp.tool(annotations=ToolAnnotations(...))` decorator MUST be copied exactly.
- Do NOT modify any function signatures or logic — this is purely a file move.
  </action>
  <verify>
    <automated>wc -l src/mnemo/mcp/tools_search.py src/mnemo/mcp/tools_books.py src/mnemo/mcp/tools_metadata.py src/mnemo/mcp/formatters.py src/mnemo/services/book_service.py && python -c "from mnemo.services.book_service import validate_epub_path, find_duplicate, list_all_books; print('service imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/mnemo/services/__init__.py exists and contains "Service layer"
    - src/mnemo/services/book_service.py contains `def validate_epub_path(path: Path) -> str | None:`
    - src/mnemo/services/book_service.py contains `def find_duplicate(book_repo: BookRepository, file_hash: str) -> Book | None:`
    - src/mnemo/services/book_service.py contains `def list_all_books(book_repo: BookRepository) -> list[Book]:`
    - src/mnemo/mcp/formatters.py contains `def _format_search_results(`
    - src/mnemo/mcp/formatters.py contains `def _format_enriched_results(`
    - src/mnemo/mcp/formatters.py contains `def _format_mixed_results(`
    - src/mnemo/mcp/formatters.py contains `def _truncate_at_boundary(`
    - src/mnemo/mcp/tools_search.py contains `def _search_books_impl(`
    - src/mnemo/mcp/tools_search.py contains `@mcp.tool`
    - src/mnemo/mcp/tools_search.py contains `from mnemo.mcp.formatters import`
    - src/mnemo/mcp/tools_books.py contains `def _add_book_impl(`
    - src/mnemo/mcp/tools_books.py contains `def _remove_book_impl(`
    - src/mnemo/mcp/tools_metadata.py contains `def _list_available_books_impl(`
    - src/mnemo/mcp/tools_metadata.py contains `def _enrich_book_impl(`
    - No single file exceeds 460 lines: `wc -l src/mnemo/mcp/tools_*.py src/mnemo/mcp/formatters.py` all under 460
  </acceptance_criteria>
  <done>All tool functions from tools.py distributed across domain modules, formatters extracted, service layer created with shared validation functions. No behavioral changes — just file reorganization.</done>
</task>

<task type="auto">
  <name>Task 2: Update server.py imports and verify tool registration</name>
  <files>
    src/mnemo/mcp/server.py
  </files>
  <read_first>
    src/mnemo/mcp/server.py,
    src/mnemo/mcp/tools_search.py,
    src/mnemo/mcp/tools_books.py,
    src/mnemo/mcp/tools_metadata.py
  </read_first>
  <action>
Update `src/mnemo/mcp/server.py` to import the three new domain modules instead of the monolithic `tools` module. The `mcp = FastMCP(...)` line MUST come BEFORE the domain imports (this prevents circular import — existing pattern, preserve it).

Replace line 28 (`import mnemo.mcp.tools  # noqa: F401, E402`) with:
```python
# Import domain tool modules to trigger @mcp.tool registration
import mnemo.mcp.tools_search    # noqa: F401, E402
import mnemo.mcp.tools_books     # noqa: F401, E402
import mnemo.mcp.tools_metadata  # noqa: F401, E402
```

Keep the old `import mnemo.mcp.tools` line as well (commented out or removed — the re-export shim in Plan 02 will handle backward compat). Actually, REMOVE the old line. The re-export shim in Plan 02 Task 1 will re-establish tools.py imports.

Verify the server starts and all 10 tools are registered by running:
```bash
python -c "from mnemo.mcp.server import mcp; print(f'Tools registered: {len(mcp._tool_manager._tools)}')"
```
Expected output: `Tools registered: 10`

If the count is wrong, check that each domain module's `@mcp.tool` decorated functions are being imported.
  </action>
  <verify>
    <automated>python -c "from mnemo.mcp.server import mcp; tools = mcp._tool_manager._tools; print(f'Count: {len(tools)}'); assert len(tools) == 10, f'Expected 10 tools, got {len(tools)}'; print('All 10 tools registered')"</automated>
  </verify>
  <acceptance_criteria>
    - src/mnemo/mcp/server.py contains `import mnemo.mcp.tools_search`
    - src/mnemo/mcp/server.py contains `import mnemo.mcp.tools_books`
    - src/mnemo/mcp/server.py contains `import mnemo.mcp.tools_metadata`
    - src/mnemo/mcp/server.py does NOT contain `import mnemo.mcp.tools  # noqa`
    - `python -c "from mnemo.mcp.server import mcp; print(len(mcp._tool_manager._tools))"` outputs 10
  </acceptance_criteria>
  <done>server.py imports the three domain modules, all 10 MCP tools register correctly, no circular import errors.</done>
</task>

</tasks>

<verification>
- `wc -l src/mnemo/mcp/tools_*.py src/mnemo/mcp/formatters.py` — all files under 460 lines
- `python -c "from mnemo.mcp.server import mcp; print(len(mcp._tool_manager._tools))"` — outputs 10
- `python -c "from mnemo.services.book_service import validate_epub_path, find_duplicate, list_all_books"` — no import error
- `uv run ruff check src/mnemo/mcp/ src/mnemo/services/` — clean
</verification>

<success_criteria>
- mcp/tools.py content fully distributed to tools_search.py, tools_books.py, tools_metadata.py, and formatters.py
- No single new file exceeds ~460 lines
- services/book_service.py exists with validate_epub_path, find_duplicate, list_all_books
- All 10 MCP tools register via server.py
- Ruff check passes on new files
</success_criteria>

<output>
After completion, create `.planning/phases/18-mcp-service-layer-refactor/18-01-SUMMARY.md`
</output>
