---
phase: 13-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-output-formatting/12-VERIFICATION.md
  - .planning/phases/11-search-filter-and-mcp-tool/11-01-SUMMARY.md
  - .planning/ROADMAP.md
  - tests/test_mcp.py
autonomous: true
requirements: [TOOL-02, SRCH-01, TOOL-01]

must_haves:
  truths:
    - "Phase 12 has a VERIFICATION.md confirming TOOL-02 is SATISFIED with line-number evidence"
    - "Phase 11 SUMMARY frontmatter lists SRCH-01 and TOOL-01 in requirements_completed"
    - "ROADMAP 12-01-PLAN checkbox is checked"
    - "test_tools_registered asserts all 8 registered tools including get_book_chunks"
    - "TestToolAnnotations docstring reflects correct tool count (eight)"
    - "test_server_imports_without_side_effects passes (startswith fix)"
    - "TestAddBookAsync tests pass (pytest-asyncio installed)"
  artifacts:
    - path: ".planning/phases/12-output-formatting/12-VERIFICATION.md"
      provides: "TOOL-02 verification report"
      contains: "TOOL-02"
    - path: ".planning/phases/11-search-filter-and-mcp-tool/11-01-SUMMARY.md"
      provides: "Updated frontmatter with requirements_completed"
      contains: "SRCH-01"
    - path: "tests/test_mcp.py"
      provides: "Fixed test assertions and docstrings"
      contains: "get_book_chunks"
  key_links:
    - from: ".planning/phases/12-output-formatting/12-VERIFICATION.md"
      to: "src/mnemo/mcp/tools.py"
      via: "Line-number citations of _format_enriched_results"
      pattern: "_format_enriched_results"
---

<objective>
Close all verification and documentation gaps identified by the v1.3 milestone audit so the milestone can pass re-audit.

Purpose: Phase 12 completed successfully but never got a VERIFICATION.md. Phase 11 SUMMARY is missing requirements_completed in frontmatter. ROADMAP has an unchecked checkbox for 12-01. Test file has stale docstrings and a missing assertion. Two sets of pre-existing test failures need fixing.

Output: VERIFICATION.md for Phase 12, corrected frontmatter/ROADMAP, fixed test assertions, all tests passing.
</objective>

<execution_context>
@/Users/joel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-search-filter-and-mcp-tool/11-VERIFICATION.md
@.planning/phases/11-search-filter-and-mcp-tool/11-01-SUMMARY.md
@.planning/phases/12-output-formatting/12-01-SUMMARY.md
@.planning/phases/13-audit-gap-closure/13-RESEARCH.md
@src/mnemo/mcp/tools.py
@tests/test_mcp.py

<interfaces>
<!-- Key evidence the executor needs for VERIFICATION.md creation -->

From src/mnemo/mcp/tools.py — _format_enriched_results function:
- Emits "\n---\n**[MATCH — seq {chunk.sequence}]**\n\n{content}" for matched chunks
- Emits "\n---\n*[Context — seq {chunk.sequence}]*\n\n{content}" for context chunks

From tests/test_mcp.py — passing tests providing TOOL-02 evidence:
- TestSearchBooksContextWindow::test_search_books_context_window_formats_enriched
- TestSearchBooksContextWindow::test_search_books_context_window_zero_unchanged
- TestSearchBooksContextWindow::test_search_books_context_window_clamped

From .planning/phases/11-search-filter-and-mcp-tool/11-VERIFICATION.md:
- Reference format for Phase 12 VERIFICATION.md (same structure, frontmatter, sections)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test assertions and pre-existing test failures</name>
  <files>tests/test_mcp.py</files>
  <action>
Make four targeted edits to tests/test_mcp.py:

1. **Line 28** — Fix server name assertion:
   Change `assert mcp.name == "mnemo"` to `assert mcp.name.startswith("mnemo")`
   Reason: Server initializes with `f"mnemo v{__version__}"` which includes the version string.

2. **Line 31** — Fix test_tools_registered docstring:
   Change `"Verify all seven tools are registered with the server."` to `"Verify all eight tools are registered with the server."`

3. **After line 42** (after `assert "get_book_structure" in tool_names`) — Add missing assertion:
   Add `assert "get_book_chunks" in tool_names`

4. **Line 46** — Fix TestToolAnnotations class docstring:
   Change `"Tests verifying ToolAnnotations on all six MCP tools."` to `"Tests verifying ToolAnnotations on all eight MCP tools."`

Then install pytest-asyncio (already declared in pyproject.toml dev dependencies):
   `pip install "pytest-asyncio>=0.23"`

Run full test suite to confirm all 5 pre-existing failures are resolved.
  </action>
  <verify>
    <automated>python -m pytest tests/test_mcp.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in test_mcp.py pass (0 failures). test_tools_registered asserts 8 tools. TestToolAnnotations docstring says "eight". test_server_imports_without_side_effects passes. TestAddBookAsync tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 12 VERIFICATION.md, fix SUMMARY frontmatter, and check ROADMAP</name>
  <files>
    .planning/phases/12-output-formatting/12-VERIFICATION.md
    .planning/phases/11-search-filter-and-mcp-tool/11-01-SUMMARY.md
    .planning/ROADMAP.md
  </files>
  <action>
**A. Create Phase 12 VERIFICATION.md** at `.planning/phases/12-output-formatting/12-VERIFICATION.md`.

Use the exact format from Phase 11's VERIFICATION.md (`.planning/phases/11-search-filter-and-mcp-tool/11-VERIFICATION.md`). The report must include:

- Frontmatter: `phase: 12-output-formatting`, `status: passed`, `score: 2/2 must-haves verified`, `re_verification: false`
- Observable Truths (2): (1) Enriched search results visually distinguish matched chunk from context neighbors with separator and label — cite `_format_enriched_results` in `tools.py` with line numbers showing `**[MATCH — seq N]**` and `*[Context — seq N]*` patterns. (2) Human can identify matched chunk at a glance — cite visual QA approval from 12-01-SUMMARY.md.
- Required Artifacts: `src/mnemo/mcp/tools.py` with `_format_enriched_results`, `tests/test_mcp.py` with `TestSearchBooksContextWindow`
- Key Links: `_format_enriched_results` called from `search_books` when `context_window > 0`
- Requirements Coverage: TOOL-02 SATISFIED with evidence from 3 passing TestSearchBooksContextWindow tests and visual QA
- Anti-Patterns: None
- Gaps Summary: No gaps

**B. Fix 11-01-SUMMARY.md frontmatter.** The file has no `requirements_completed` field. Add `requirements-completed: [SRCH-01, TOOL-01]` to the YAML frontmatter block. Use the hyphen form (`requirements-completed`) to match the Phase 10 SUMMARY convention, but check the actual file first — the `provides` field in `dependency_graph` already lists them, so also add as a top-level key alongside `decisions` and `metrics`.

**C. Fix ROADMAP.md line 89.** Change `- [ ] 12-01-PLAN.md` to `- [x] 12-01-PLAN.md` (the plan is complete).
  </action>
  <verify>
    <automated>test -f .planning/phases/12-output-formatting/12-VERIFICATION.md && grep -c "TOOL-02" .planning/phases/12-output-formatting/12-VERIFICATION.md && grep -c "SRCH-01" .planning/phases/11-search-filter-and-mcp-tool/11-01-SUMMARY.md && grep "\[x\].*12-01-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <done>Phase 12 VERIFICATION.md exists with TOOL-02 SATISFIED and line-number evidence. 11-01-SUMMARY.md frontmatter contains requirements-completed with SRCH-01 and TOOL-01. ROADMAP 12-01-PLAN checkbox is checked.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite to confirm zero failures:

```bash
python -m pytest tests/ --tb=short -q
```

Confirm all documentation artifacts exist and contain expected content:

```bash
grep "SATISFIED" .planning/phases/12-output-formatting/12-VERIFICATION.md
grep "SRCH-01" .planning/phases/11-search-filter-and-mcp-tool/11-01-SUMMARY.md
grep "\[x\].*12-01-PLAN" .planning/ROADMAP.md
```
</verification>

<success_criteria>
1. Full test suite passes with 0 failures (the 5 pre-existing failures are resolved)
2. Phase 12 VERIFICATION.md exists and confirms TOOL-02 SATISFIED with specific code citations
3. Phase 11 SUMMARY frontmatter lists SRCH-01 and TOOL-01 in requirements-completed
4. ROADMAP 12-01 checkbox is checked
5. test_tools_registered asserts all 8 tools including get_book_chunks
6. TestToolAnnotations docstring says "eight"
</success_criteria>

<output>
After completion, create `.planning/phases/13-audit-gap-closure/13-01-SUMMARY.md`
</output>
