layout python python3

dotenv_if_exists "$HOME/.config/helios/secrets"

# Semantic search needs an OpenAI-compatible embeddings endpoint (see .env.example).
# export MNEMO_EMBED_BASE_URL=https://api.openai.com/v1
# export MNEMO_EMBED_API_KEY=$OPENAI_API_KEY
