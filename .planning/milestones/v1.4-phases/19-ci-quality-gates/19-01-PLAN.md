---
phase: 19-ci-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_embedding_integration.py
  - .github/workflows/ci.yml
  - README.md
autonomous: true
requirements:
  - CICD-01
  - CICD-02
  - CICD-03
  - CICD-04

must_haves:
  truths:
    - "Pushing to main triggers a GitHub Actions workflow that runs ruff, mypy, and pytest"
    - "Opening a PR triggers the same workflow"
    - "CI tests run against both Python 3.11 and 3.12"
    - "Integration tests requiring Databricks credentials are excluded from CI runs"
    - "Build fails if coverage drops below 80%"
    - "Build fails if ruff or mypy report errors"
    - "README displays a CI status badge"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline configuration"
      contains: "on:"
    - path: "pyproject.toml"
      provides: "Registered pytest integration marker"
      contains: "integration: marks tests"
    - path: "tests/test_embedding_integration.py"
      provides: "Module-level pytestmark excluding from CI"
      contains: "pytestmark = pytest.mark.integration"
    - path: "README.md"
      provides: "CI status badge"
      contains: "badge.svg"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "uv sync reads lockfile and pyproject config"
      pattern: "uv sync --locked"
    - from: ".github/workflows/ci.yml"
      to: "tests/test_embedding_integration.py"
      via: "pytest -m 'not integration' excludes marked tests"
      pattern: "-m 'not integration'"
---

<objective>
Create a GitHub Actions CI pipeline that validates every push and PR with linting, type checking, testing, and coverage enforcement. Add a CI status badge to README.md.

Purpose: Automate quality gates so regressions are caught before merging. This is the final phase of v1.4 tech debt cleanup.
Output: `.github/workflows/ci.yml`, updated `pyproject.toml` (marker registration), updated `tests/test_embedding_integration.py` (pytestmark), updated `README.md` (badge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-ci-quality-gates/19-CONTEXT.md
@.planning/phases/19-ci-quality-gates/19-RESEARCH.md

@pyproject.toml
@README.md
@tests/test_embedding_integration.py
@Makefile

<interfaces>
<!-- Existing pyproject.toml pytest config (lines 53-59): -->
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"
filterwarnings = [
    "ignore::bs4.XMLParsedAsHTMLWarning",
]

<!-- test_embedding_integration.py has no existing pytestmark. Classes: TestEmbedBook, likely others. -->
<!-- README.md starts with: # Mnemo -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register integration marker, apply pytestmark, and create CI workflow</name>
  <files>pyproject.toml, tests/test_embedding_integration.py, .github/workflows/ci.yml</files>
  <read_first>
    - pyproject.toml (current [tool.pytest.ini_options] section — lines 53-59)
    - tests/test_embedding_integration.py (full file — need to see current imports and structure)
    - Makefile (existing quality gate commands for reference)
  </read_first>
  <action>
**Step 1: Register pytest marker in pyproject.toml (per D-04)**

In the `[tool.pytest.ini_options]` section of `pyproject.toml`, add a `markers` list after the existing `filterwarnings`:

```toml
markers = [
    "integration: marks tests requiring external credentials (deselect with '-m not integration')",
]
```

The full section should read:
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"
filterwarnings = [
    "ignore::bs4.XMLParsedAsHTMLWarning",
]
markers = [
    "integration: marks tests requiring external credentials (deselect with '-m not integration')",
]
```

**Step 2: Add pytestmark to test_embedding_integration.py (per D-04)**

Add `pytestmark = pytest.mark.integration` at module level, after the existing imports. The file already imports `pytest`, so just add the line after the imports block (after line 8 which has `from mnemo.ingest import ...`). Insert:

```python
pytestmark = pytest.mark.integration
```

Place it after the last import line and before the `SAMPLE_EPUB` constant. Do NOT remove any existing code.

**Step 3: Create .github/workflows/ci.yml (per D-01, D-02, D-03)**

Create the directory `.github/workflows/` if it doesn't exist.

Create `.github/workflows/ci.yml` with this exact content:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - run: uv sync --locked --all-extras --dev
      - run: uv run ruff check src/ tests/
      - run: uv run ruff format --check src/ tests/
      - run: uv run mypy src/

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
      - run: uv sync --locked --all-extras --dev
      - run: uv run pytest -m 'not integration' --cov=mnemo --cov-report=term-missing --cov-fail-under=80
```

Key decisions embedded:
- Two parallel jobs: `lint` and `test` (Claude's discretion — independent signals)
- `astral-sh/setup-uv@v6` with `enable-cache: true` (Claude's discretion — official uv caching)
- Inline commands rather than make targets (Claude's discretion — more transparent in CI logs)
- Python matrix: 3.11 and 3.12 (per D-01)
- Triggers: push to main + all PRs (per D-03)
- Coverage threshold: 80% via `--cov-fail-under=80` (per D-02)
- Integration exclusion: `-m 'not integration'` (per D-04)
  </action>
  <verify>
    <automated>cd /Users/joel/Code/mnemo && uv run pytest --co -q -m 'not integration' 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains the line: `"integration: marks tests requiring external credentials (deselect with '-m not integration')"`
    - tests/test_embedding_integration.py contains `pytestmark = pytest.mark.integration` before any class or function definition
    - .github/workflows/ci.yml exists and contains `name: CI`
    - .github/workflows/ci.yml contains `branches: [main]`
    - .github/workflows/ci.yml contains `pull_request:`
    - .github/workflows/ci.yml contains `python-version: ["3.11", "3.12"]`
    - .github/workflows/ci.yml contains `--cov-fail-under=80`
    - .github/workflows/ci.yml contains `-m 'not integration'`
    - .github/workflows/ci.yml contains `ruff check src/ tests/`
    - .github/workflows/ci.yml contains `ruff format --check src/ tests/`
    - .github/workflows/ci.yml contains `mypy src/`
    - `uv run pytest --co -q -m 'not integration'` collects ~517 tests (not 525)
    - `uv run pytest -m 'not integration' -x -q` exits 0
  </acceptance_criteria>
  <done>Pytest integration marker registered and applied, CI workflow file created with lint job (ruff + mypy) and test job (pytest matrix with coverage gate). Running pytest with -m 'not integration' excludes the 8 integration tests and all remaining tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add CI badge to README and commit uv.lock</name>
  <files>README.md, uv.lock</files>
  <read_first>
    - README.md (first 5 lines — need to see the heading to insert badge after it)
  </read_first>
  <action>
**Step 1: Add CI badge to README.md (per CICD-04)**

Insert the badge on line 3 of README.md, immediately after the `# Mnemo` heading (line 1) and before the blank line. The result should be:

```markdown
# Mnemo

![CI](https://github.com/joel-eq/mnemo/actions/workflows/ci.yml/badge.svg)

Personal technical book library with semantic search via MCP.
```

The badge URL is `https://github.com/joel-eq/mnemo/actions/workflows/ci.yml/badge.svg` — this uses the exact repo slug `joel-eq/mnemo` and workflow filename `ci.yml`.

**Step 2: Stage uv.lock**

The `uv.lock` file is currently modified (shown in git status as `M uv.lock`). CI runs `uv sync --locked` which requires the lockfile to match. Stage and include `uv.lock` in the commit so CI doesn't fail with "Lockfile is out of date".

Run: `uv lock --check` to verify the lock file is consistent, then stage it.
  </action>
  <verify>
    <automated>cd /Users/joel/Code/mnemo && grep -c "badge.svg" README.md && grep -c "joel-eq/mnemo" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `![CI](https://github.com/joel-eq/mnemo/actions/workflows/ci.yml/badge.svg)`
    - The badge line appears after `# Mnemo` and before the description paragraph
    - uv.lock is staged or committed (not left as modified)
  </acceptance_criteria>
  <done>README.md displays a CI status badge linking to the GitHub Actions workflow. uv.lock is committed so CI's `uv sync --locked` will succeed.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Marker exclusion works:** `uv run pytest --co -q -m 'not integration' 2>&1 | tail -1` shows ~517 tests collected (not 525)
2. **All non-integration tests pass with coverage:** `uv run pytest -m 'not integration' --cov=mnemo --cov-report=term-missing --cov-fail-under=80` exits 0
3. **Linting passes:** `uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/` exits 0
4. **Type checking passes:** `uv run mypy src/` exits 0
5. **Workflow file is valid YAML:** `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
6. **Badge in README:** `grep 'badge.svg' README.md` matches
</verification>

<success_criteria>
- `.github/workflows/ci.yml` exists with lint and test jobs
- Pushing to main or opening a PR will trigger the workflow (verified by file content, live verification after push)
- `pytest -m 'not integration'` excludes integration tests
- Coverage threshold of 80% enforced via `--cov-fail-under=80`
- README.md shows CI badge
- All existing tests pass with no behavior changes
</success_criteria>

<output>
After completion, create `.planning/phases/19-ci-quality-gates/19-01-SUMMARY.md`
</output>
